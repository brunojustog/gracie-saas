# Banco
DATABASE_URL="postgresql://gracie:gracie@localhost:5433/gracie_saas?schema=public"

# Auth.js v5
# Gere um secret com: openssl rand -base64 32  (ou: npx auth secret)
AUTH_SECRET=""
AUTH_TRUST_HOST="true"
NEXTAUTH_URL="http://localhost:3000"

# Resend (magic links / emails transacionais) — fase posterior
RESEND_API_KEY=""
EMAIL_FROM="Gracie SaaS <noreply@example.com>"

# Seed (override se quiser)
SEED_ADMIN_EMAIL="bruno@simplificaonline.site"
SEED_ADMIN_PASSWORD="gracie-2026"
